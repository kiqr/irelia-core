  <section class="flex flex-col gap-8 items-center justify-center w-full">
    <header class="flex flex-col items-center gap-y-2 text-center max-w-lg">
      <h1 class="font-bold font-headings text-neutral-600 dark:text-neutral-100 text-2xl"><%= title %></h1>
      <p class="text-neutral-400 dark:text-neutral-200"><%= description %></p>
    </header>

    <%= content %>
  </section>
